AppBundle\Entity\Affiche:
  affiche1{1..20}:
    dateTime: <dateTimeBetween('-61 days', '-31 days')>
    createdAt: <dateTimeBetween('-200 days', 'now')>
    updatedAt: <dateTimeBetween($createdAt, 'now')>
    deletedAt: <dateTimeBetween($createdAt, 'now')>

AppBundle\Entity\Affiche:
  affiche2{1..20}:
    dateTime: <dateTimeBetween(@affiche1-> dateTime, 'now')>
    createdAt: <dateTimeBetween('-200 days', 'now')>
    updatedAt: <dateTimeBetween($createdAt, 'now')>
    deletedAt: <dateTimeBetween($createdAt, 'now')>

AppBundle\Entity\Affiche:
  affiche3{1..20}:
    dateTime: dateTime: <dateTimeBetween('now', '+30 days')>
    createdAt: <dateTimeBetween('-200 days', 'now')>
    updatedAt: <dateTimeBetween($createdAt, 'now')>
    deletedAt: <dateTimeBetween($createdAt, 'now')>

AppBundle\Entity\Affiche:
  affiche4{1..20}:
    dateTime: dateTime: <dateTimeBetween(@affiche1-> dateTime, '+31 days')>
    createdAt: <dateTimeBetween('-200 days', 'now')>
    updatedAt: <dateTimeBetween($createdAt, 'now')>
    deletedAt: <dateTimeBetween($createdAt, 'now')>
