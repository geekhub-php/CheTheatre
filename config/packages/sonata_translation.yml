#imports:
#    - { resource: '@SonataTranslationBundle/Resources/config/routes.yaml'}

sonata_translation:
    locales: ['uk', 'en']
    default_locale: "uk"
    # here enable the types you need
    gedmo:
        enabled: true
